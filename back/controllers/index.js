const reviewControllers = require('./reviewControllers')
const userControllers = require('./userControllers')
const favouriteControllers = require('./favouriteControllers')

module.exports = {
    userControllers,
    reviewControllers,
    favouriteControllers
}